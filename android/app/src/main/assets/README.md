# Assets

The Android scaffold uses `res/drawable/ic_medtrack_mark.xml` as a vector placeholder.

When final branding is available, add source SVG/PNG files here or replace the launcher vector with Android-ready drawable assets generated from the approved MEDTRACK mark. Do not include PHI in screenshots or seed assets.
